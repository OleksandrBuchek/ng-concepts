import { WithProvidersDirective } from './with-providers.directive';

describe('WithProvidersDirective', () => {
  it('should create an instance', () => {
    const directive = new WithProvidersDirective();
    expect(directive).toBeTruthy();
  });
});
